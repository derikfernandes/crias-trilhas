/**
 * API pública de stages para componentes visuais.
 * Reexporta wrappers existentes sem alterar comportamento.
 */
export {
  createTrailStage,
  deleteTrailStage,
  updateTrailStage,
  type TrailStageCreateData,
  type TrailStageUpdateData,
} from '../trailStageFirestore'
